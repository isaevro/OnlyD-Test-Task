import { FC } from 'react'
import * as S from './styles'
import * as C from '../../styles/components'

export const Header: FC = () => {
  return (
    <S.Header>
      <C.Title1>ONLY.</C.Title1>
    </S.Header>
  )
}
